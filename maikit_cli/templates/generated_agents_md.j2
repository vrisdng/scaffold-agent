# {{ agent_name }}

This generated agent was created by MaiKit.

Run it locally with:

```bash
maikit run {{ agent_name }} "pricing-api latency spiked after latest deploy"
maikit eval {{ agent_name }}
```

The source of truth is `agent.yaml`. Keep prompt changes versioned under
`prompts/`, policy changes under `policies/`, and eval changes under `evals/`.
